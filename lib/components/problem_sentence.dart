import 'package:flutter/material.dart';

class ProblemSentence extends StatelessWidget {
  const ProblemSentence({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("ProblemSentence"),
    );
  }
}
